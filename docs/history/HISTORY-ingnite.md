# HISTORY

```sh
npx ignite-cli@latest new RN23ignite  
```

```out
✅ What bundle identifier? · com.yoox.rn23ignite
✅ Where do you want to start your project? · /Users/coconm/wa/tma24_sandox/app/src_rn23/RN23ignite
✅ Choose a workflow: · Expo Go
✅ Do you want to initialize a git repository? (Y/n) · No
✅ Remove demo code? We recommend leaving it in if it's your first time using Ignite (y/N) · Yes
✅ Which package manager do you want to use? · yarn
✅ Do you want to install dependencies? (Y/n) · Yes
..
Ignited RN23ignite in 191.11s  🚀 

For next time: here are the Ignite options you picked!
    npx ignite-cli new RN23ignite \
    --bundle=com.yoox.rn23ignite \
    --git=false \
    --install-deps \
    --packager=yarn \
    --target-path=/Users/coconm/wa/tma24_sandox/app/src_rn23/RN23ignite \
    --remove-demo \
    --workflow=expo \
    --no-timeout=false 
```

```sh
yarn ios
```

Let’s create the MobX State Tree (MST) model:

```sh
npx ignite-cli generate model yhome
```
